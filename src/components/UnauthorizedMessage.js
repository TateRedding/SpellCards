import React from "react";

const UnauthorizedMessage = () => {
    return (
        <h3 className="text-center text-danger">You do not have access to this page!</h3>
    );
};

export default UnauthorizedMessage;